#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main()
{
    int ST;
    string input;
    vector<int> arr_int;
    while (cin >> input)
    {
        for (int ST = 1; ST < 10000; ST++)
        {
            for (int i = 0; i < input.length(); i++)
            {
                int num = (ST + (input[i] - 'A'));
                while (num /)
            }
        }
    }

    return 0;
}